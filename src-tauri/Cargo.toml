[package]
name = "vessel"
version = "0.1.0"
edition = "2021"
default-run = "vessel"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
vessel-core = { path = "../crates/vessel-core" }
serde_json = "1"
